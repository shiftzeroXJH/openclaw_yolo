interface:
  display_name: "OpenClaw YOLO Agent"
  short_description: "Operate local YOLO training through the CLI."
  default_prompt: "Use $openclaw-yolo-agent to manage YOLO tasks through the local openclaw-yolo CLI."

policy:
  allow_implicit_invocation: true
